/**
 * Placeholder type for the canary types tier.
 * Replace with real domain types as they are promoted from extras.
 */
export interface CanaryTypePlaceholder {
  readonly _brand: 'CanaryTypePlaceholder';
}
