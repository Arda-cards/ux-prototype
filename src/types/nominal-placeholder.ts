/**
 * Placeholder type for the nominal (stable) types tier.
 * Replace with real domain types as they graduate from canary.
 */
export interface NominalTypePlaceholder {
  readonly _brand: 'NominalTypePlaceholder';
}
