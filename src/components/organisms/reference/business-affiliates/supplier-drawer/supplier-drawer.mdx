import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as SupplierDrawerStories from './supplier-drawer.stories';

<Meta of={SupplierDrawerStories} />

# ArdaSupplierDrawer

A slide-in drawer for viewing, adding, or editing a business affiliate (supplier). Features a tabbed view mode with Details and Items tabs, form mode with collapsible sections for legal and contact information, and dirty-state protection.

## View Mode

<Canvas of={SupplierDrawerStories.ViewMode} />

<Controls of={SupplierDrawerStories.ViewMode} />

## View Mode — Items Tab

<Canvas of={SupplierDrawerStories.ViewModeItemsTab} />

## Add Mode

<Canvas of={SupplierDrawerStories.AddMode} />

## Edit Mode

<Canvas of={SupplierDrawerStories.EditMode} />

## Interactive

Demonstrates the full view-to-edit flow with form submission.

<Canvas of={SupplierDrawerStories.Interactive} />
